import React, { Component } from "react";
import { withStyles } from "@material-ui/core/styles";
import styles from "./styles";

class Pages extends Component {
  render() {
    return <div>welcome Pages</div>;
  }
}

export default withStyles(styles)(Pages);
